# Server Configuration
server.port=8080

# Service URLs
service2.url=http://localhost:8081
service3.url=http://localhost:8083

# Logging
logging.level.com.example.service1=INFO
logging.level.org.springframework.web=INFO

# Application Name
spring.application.name=service1-orchestrator
